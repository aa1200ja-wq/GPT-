# Codex Cloud 執行回報範本

> Codex 完成目前唯一已批准任務後，必須更新本文件並立即停止。  
> 所有欄位都必須依實際結果填寫，不得保留「尚未執行」或其他預設文字。

## 一、任務基本資料

- 任務編號：`<TASK_ID>`
- 任務名稱：`<TASK_NAME>`
- 執行狀態：`<已完成／部分完成／已停止>`
- 儲存庫：`<OWNER>/<REPOSITORY>`
- 基準分支：`<BASE_BRANCH>`
- Codex Cloud 工作分支：`<WORK_BRANCH>`
- Pull Request：`<PR_URL_OR_PENDING>`

若平台尚未建立 Pull Request，填寫：

`待 Codex Cloud 介面建立`

## 二、執行結果摘要

`<用一至三句話說明本次任務實際完成結果。>`

## 三、完成內容

- `<完成內容一>`
- `<完成內容二>`
- `<完成內容三>`

沒有完成內容時，必須說明停止原因，不得留白。

## 四、修改檔案

- `<MODIFIED_FILE_PATH_1>`
- `<MODIFIED_FILE_PATH_2>`

沒有修改檔案時填寫：

`無。`

## 五、新增檔案

- `<ADDED_FILE_PATH_1>`
- `<ADDED_FILE_PATH_2>`

沒有新增檔案時填寫：

`無。`

## 六、刪除檔案

- `<DELETED_FILE_PATH_1>`

沒有刪除檔案時填寫：

`無。`

## 七、移動或重新命名檔案

- `<OLD_PATH>` → `<NEW_PATH>`

沒有移動或重新命名檔案時填寫：

`無。`

## 八、測試結果

### 測試一

- 測試指令：`<TEST_COMMAND_1>`
- 測試結果：`<通過／失敗／無法執行>`
- 結果說明：`<DETAILS>`

### 測試二

- 測試指令：`<TEST_COMMAND_2>`
- 測試結果：`<通過／失敗／無法執行>`
- 結果說明：`<DETAILS>`

### 變更範圍確認

- 執行指令：`git status --short`
- 是否只包含任務允許的檔案：`<是／否>`
- 實際變更檔案：
  - `<FILE_PATH_1>`
  - `<FILE_PATH_2>`

不適用的測試區塊必須刪除，不得保留未替換的佔位文字。

## 九、驗收條件檢查

- 任務目標是否完成：`<是／否／部分完成>`
- 是否只修改允許的檔案：`<是／否>`
- 是否修改禁止範圍：`<是／否>`
- 是否操作其他儲存庫：`<是／否>`
- 是否新增未授權套件：`<是／否>`
- 是否寫入機密資料：`<是／否>`
- 是否破壞既有功能：`<是／否／無法確認>`
- 是否更新本回報：`<是／否>`
- 是否保留 Codex Cloud 平台變更：`<是／否>`
- 是否已停止繼續執行：`<是／否>`

任何項目填寫「否」或「無法確認」時，必須在問題與風險中說明。

## 十、未完成項目

- `<UNFINISHED_ITEM_1>`
- `<UNFINISHED_ITEM_2>`

全部完成時填寫：

`無。`

## 十一、問題與風險

- `<ISSUE_OR_RISK_1>`
- `<ISSUE_OR_RISK_2>`

沒有已知問題與風險時填寫：

`無已知問題與風險。`

## 十二、需要 GPT／小安／老闆確認的事項

- `<CONFIRMATION_ITEM_1>`
- `<CONFIRMATION_ITEM_2>`

沒有額外確認事項時填寫：

`請 GPT／小安驗收目前任務；未經驗收及老闆說「通過」，不得執行下一個任務。`

## 十三、Codex Cloud 交付狀態

- 已使用平台提供的工作分支：`<是／否>`
- 已更新本回報：`<是／否>`
- 已保留平台產生的變更：`<是／否>`
- Pull Request 狀態：`<待 Codex Cloud 介面建立／已建立／建立失敗>`
- Pull Request 網址：`<PR_URL_OR_NONE>`
- 已停止繼續執行：`<是／否>`

## 十四、停止或失敗原因

任務正常完成時填寫：

`無。`

任務未完成或被迫停止時，必須明確填寫：

- 停止階段：`<STAGE>`
- 停止原因：`<REASON>`
- 已完成內容：`<COMPLETED_BEFORE_STOP>`
- 未完成內容：`<UNFINISHED_AFTER_STOP>`
- 是否修改任何檔案：`<是／否>`
- 是否需要老闆或 GPT 補充資料：`<是／否>`
- 所需補充資料：`<DETAILS_OR_NONE>`

## 十五、最終停止確認

完成本回報後，Codex 必須確認：

- 未自行執行 `git fetch`
- 未自行建立、切換或重新命名分支
- 未自行推送或合併至基準分支
- 未自行修改 `handoff/task.md`
- 未自行更改任務批准狀態
- 未自行執行下一個任務
- 未操作未授權的其他儲存庫
- 已完成回報並立即停止

未經 GPT／小安驗收及老闆說「通過」，不得執行下一個任務。
